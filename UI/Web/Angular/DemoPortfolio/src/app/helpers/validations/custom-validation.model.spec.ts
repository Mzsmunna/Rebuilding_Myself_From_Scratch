import { CustomValidation } from './custom-validation.model';

describe('CustomValidation', () => {
  it('should create an instance', () => {
    expect(new CustomValidation()).toBeTruthy();
  });
});
